// ===================================================================================================
// [NOTE]
// - Esbuild로 단일파일 빌드를 할 때는 ESM으로 빌드하면 CJS로 된 패키지들이 문제를 발생시킴
// - 따라서 CJS로 빌드하도록 src/app.cts 파일을 별도로 작성
// ===================================================================================================

export function app() {
    console.log("Success");
}

// CJS
if (require.main === module) {
    app();
}