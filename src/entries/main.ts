// ===================================================================================================
// [NOTE]
// - ESModule
// - 프로그램적 진입점이 되는 파일
// - app.ts를 참조
// - 별도 수정 필요 없음
// ===================================================================================================
import { app } from "../app";

if (import.meta.url?.endsWith(process.argv[1].replace(/\\/g, '/'))) {
    app();
}